import { UpperCapitalPipe } from './upper-capital.pipe';

describe('UpperCapitalPipe', () => {
  it('create an instance', () => {
    const pipe = new UpperCapitalPipe();
    expect(pipe).toBeTruthy();
  });
});
